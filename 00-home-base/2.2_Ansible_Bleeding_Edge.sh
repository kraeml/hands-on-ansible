   git clone https://github.com/ansible/ansible
   cd ansible/
   git branch 
   git submodule update --init --recursive
   git pull
   git submodule update --init --recursive
   sudo apt-get install python-jinja2 python-paramiko python-yaml sshpass python-httplib2
   which ansible
   source ./hacking/env-setup
   which ansible
   ansible --version
   exit
